# Longitude Geo Intelligence - MVP 1.6 Corrigido

## Correção

- Corrigido erro de sintaxe em `src/App.jsx` na função `exportarConsultaGeoJSON`.
- Mantida correção do relatório Word DOCX.

## Atualizar projeto Git

Copie os arquivos desta versão para a pasta Git do projeto e rode:

```cmd
npm install
npm run build
git add .
git commit -m "Corrigir erro build relatorio Word"
git push
```
